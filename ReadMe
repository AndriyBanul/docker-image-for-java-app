to build image:
docker build -t spring-boot-docler-app .

To run container with image:
docker run -p 8080:8080 spring-boot-docler-app

to run docker-compose:
docker-compose up --build

to stop compose:
docker-compose stop